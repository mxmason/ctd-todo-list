import preview from "#storybook/preview.tsx";

import { Layout } from "./Layout.tsx";

const meta = preview.meta({ component: Layout, tags: ["ai-generated"] });

export const Default = meta.story({});
